/**
 * Факты о наборе, которые повторяются по всей странице.
 *
 * До выноса дата старта была написана в пяти файлах, а номер потока — в семи.
 * Перенос набора означал бы двенадцать правок, и любая забытая молча оставила бы
 * лендинг врать. Здесь — один источник правды.
 *
 * Формулировки взяты из Maket.html дословно; при подстановке текст на странице
 * должен совпасть с макетом символ в символ.
 */

/** Номер потока. Встречается в кикере, шапке, тарифах, sticky-кнопке и мете. */
export const COHORT = 6;

/** Дата старта в именительном виде: «Старт 10 сентября 2026». */
export const START_DATE = '10 сентября 2026';

/** Дата старта в родительном: «Старт 6 потока — 10 сентября 2026». */
export const START_DATE_GENITIVE = START_DATE;

/** Текст главной кнопки. Повторяется в герое, мобильном меню и sticky-баре. */
export const CTA_PRIMARY = `Записаться в ${COHORT} поток`;

/**
 * Цифры школы. Дублируются в трастбаре (счётчики) и в биографии Марины
 * в секции «Эксперты». Правка здесь меняет оба места.
 */
export const stats = {
  /** учеников школы */
  students: 3000,
  /** восстановили здоровье с Мариной лично */
  restored: 300,
  /** практикующих нутрициологов */
  practitioners: 200,
} as const;
